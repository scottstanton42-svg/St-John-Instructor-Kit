(()=>{
const style=document.createElement('style');style.textContent=`
#todayFlow,#legacyTeachingCard{display:none!important}
#wfDetails{display:block!important;margin:0 0 14px!important;background:#242d35!important;border:1px solid #3d4a55!important;border-radius:18px!important;padding:20px!important;color:#f3f6f8}
#wfDetails .course-meta-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:14px}
#wfDetails .course-meta-item{background:#1b252e;border:1px solid #394650;border-radius:12px;padding:11px}
#wfDetails .course-meta-item small{display:block;color:#aeb8c2;font-size:11px;text-transform:uppercase;letter-spacing:.06em;margin-bottom:4px}
#wfDetails .course-meta-item strong{display:block;color:#f3f6f8;overflow-wrap:anywhere}
#todayPhaseCard{margin-top:14px}
#todayPhaseCard .phase-banner{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:14px}
#todayPhaseCard .phase-banner h3{font-size:22px}
#todayPhaseCard .phase-banner small{display:block;color:var(--muted);margin-top:4px}
#todayPhaseCard .phase-status{font-size:12px;font-weight:800;padding:8px 11px;border-radius:999px;background:var(--blue-soft);color:#78baf0;white-space:nowrap}
#todayPhaseCard .phase-status.complete{background:#163b2d;color:#5ee09b}
#todayPhaseCard .phase-collapse{width:48px;height:48px;border:1px solid #43515d;background:#202a33;color:#d8e0e6;border-radius:10px;font-weight:900;font-size:20px;touch-action:manipulation;cursor:pointer;flex:0 0 auto}
#todayPhaseCard .phase-collapse:hover{background:var(--card-light)}
#todayPhaseCard .phase-row{display:flex;align-items:center;gap:12px;padding:14px 4px;border-top:1px solid #394650;min-height:66px}
#todayPhaseCard .phase-row:first-child{border-top:0}
#todayPhaseCard .phase-row input[type=checkbox]{width:32px;height:32px;min-width:32px;accent-color:#35c978;cursor:pointer;touch-action:manipulation}
#todayPhaseCard .phase-row.done .phase-label{text-decoration:line-through;color:#7f8993}
#todayPhaseCard .phase-label{flex:1;font-size:18px;line-height:1.35;font-weight:750}
#todayPhaseCard .phase-detail{display:block;color:var(--muted);font-size:15px;font-weight:400;margin-top:3px}
#todayPhaseCard .phase-controls{display:flex;gap:5px}
#todayPhaseCard .phase-controls button{width:48px;height:48px;border:1px solid #43515d;background:#202a33;color:#d8e0e6;border-radius:10px;font-weight:900;font-size:18px;touch-action:manipulation}
#todayPhaseCard .phase-controls button:hover{background:var(--card-light)}
#todayPhaseCard .phase-controls .phase-delete{color:#ff9a9f;border-color:#684047;background:#2b2529}
#todayPhaseCard .phase-actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px;padding-top:16px;border-top:1px solid #394650}
#todayPhaseCard .phase-actions .select-all{background:#202a33;border:1px solid #43515d;color:#d8e0e6}
#todayPhaseCard .phase-complete-message{padding:18px;border-radius:14px;background:#163b2d;border:1px solid #2d805a;color:#d8ffe9;font-weight:750}
#wfPhase{background:#0e1419!important;color:#f3f6f8!important}
#wfPhase::before{content:"";position:fixed;inset:0;background-image:url('https://stjohn.org.au/app/uploads/2025/08/SJA_T_AT_WhatIs.jpg');background-size:cover;background-position:center;opacity:.12;pointer-events:none;z-index:-1}
#wfPhase .wf{position:relative;z-index:1}
#wfPhase .wcard{background:#242d35!important;border:1px solid #3d4a55!important;color:#f3f6f8!important;box-shadow:0 10px 30px rgba(0,0,0,.28)!important}
#wfPhase .wbtn.wp{background:#d71920;color:#fff}
#wfPhase .wbtn.ws{background:#202a33;color:#d8e0e6;border:1px solid #43515d}
@media(max-width:800px){#wfDetails .course-meta-grid{grid-template-columns:1fr 1fr}#todayPhaseCard .phase-banner{align-items:flex-start;flex-direction:column}}
`;
document.head.appendChild(style);
state.deletedTopics=state.deletedTopics||{};state.deletedPackDown=state.deletedPackDown||{};state.todayCollapsed=state.todayCollapsed||{};
function deleted(set,key){return !!(set&&set[key])}
function topicItems(){const base=(course().topics||[]).map((x,i)=>({value:x,key:'b'+i}));const custom=(state.customTopics[state.course]||[]).map((x,i)=>({value:x,key:'c'+i}));const gone=state.deletedTopics[state.course]||{};return [...base,...custom].filter(x=>!deleted(gone,x.key))}
function packItems(){const base=(packDownByCourse[state.course]||defaultPackDown).map((x,i)=>({value:x,key:'b'+i}));const custom=(state.customPackDown[state.course]||[]).map((x,i)=>({value:x,key:'c'+i}));const gone=state.deletedPackDown[state.course]||{};return [...base,...custom].filter(x=>!deleted(gone,x.key))}
function prepComplete(){const a=equipmentList();return a.length>0&&a.every(x=>x.done)}
function teachComplete(){const a=topicItems(),c=state.checks[state.course]||{};return a.length>0&&a.every((_,i)=>!!c[i])}
function packComplete(){const a=packItems(),c=state.packChecks[state.course]||{};return a.length>0&&a.every((_,i)=>!!c[i])}
function currentPhase(){if(!prepComplete())return 'prep';if(!teachComplete())return 'teach';if(!packComplete())return 'pack';return 'done'}
function addItem(kind){const label=kind==='prep'?'Name the new preparation item:':kind==='teach'?'Name the new teaching item:':'Name the new pack-down item:';const name=prompt(label);if(!name||!name.trim())return;if(kind==='prep')equipmentList().push({text:name.trim(),done:false});else if(kind==='teach'){state.customTopics[state.course]=state.customTopics[state.course]||[];state.customTopics[state.course].push([name.trim(),''])}else{state.customPackDown[state.course]=state.customPackDown[state.course]||[];state.customPackDown[state.course].push(name.trim())}save()}
function toggle(kind,index,checked){if(kind==='prep'){const a=equipmentList();if(a[index])a[index].done=checked}else if(kind==='teach'){state.checks[state.course]=state.checks[state.course]||{};state.checks[state.course][index]=checked}else{state.packChecks[state.course]=state.packChecks[state.course]||{};state.packChecks[state.course][index]=checked}save()}
function selectAll(kind){if(kind==='prep'){equipmentList().forEach(x=>x.done=true)}else if(kind==='teach'){const a=topicItems();state.checks[state.course]=state.checks[state.course]||{};a.forEach((_,i)=>state.checks[state.course][i]=true)}else{const a=packItems();state.packChecks[state.course]=state.packChecks[state.course]||{};a.forEach((_,i)=>state.packChecks[state.course][i]=true)}save()}
function shiftChecks(store,index){const next={};Object.keys(store||{}).forEach(k=>{const n=Number(k);if(Number.isNaN(n)||n===index)return;next[n>index?n-1:n]=store[k]});return next}
function deleteItem(kind,index){if(!confirm('Delete this checklist item? It cannot be edited once created.'))return;if(kind==='prep'){equipmentList().splice(index,1)}else if(kind==='teach'){const item=topicItems()[index];if(!item)return;state.deletedTopics[state.course]=state.deletedTopics[state.course]||{};state.deletedTopics[state.course][item.key]=true;state.checks[state.course]=shiftChecks(state.checks[state.course]||{},index)}else{const item=packItems()[index];if(!item)return;state.deletedPackDown[state.course]=state.deletedPackDown[state.course]||{};state.deletedPackDown[state.course][item.key]=true;state.packChecks[state.course]=shiftChecks(state.packChecks[state.course]||{},index)}save()}
function reorder(kind,index,dir){if(typeof moveItem==='function')moveItem(kind==='prep'?'equipment':kind==='teach'?'teaching':'pack',index,dir)}
function toggleCollapsed(phase){const key=state.course+'-'+phase;state.todayCollapsed[key]=!state.todayCollapsed[key];save()}
function transition(title,message,nextLabel,nextPhase){let old=document.getElementById('wfPhase');if(old)old.remove();const e=document.createElement('div');e.id='wfPhase';e.innerHTML=`<div class="wf"><div class="wfbrand"><img src="optimise.webp" alt="St John"><h1>${esc(title)}</h1><p>${esc(message)}</p></div><div class="wcard"><h2>Ready to continue?</h2><p>${esc(nextLabel)}</p><button class="wbtn wp" id="phaseNext">${esc(nextLabel)}</button></div></div>`;document.body.appendChild(e);e.querySelector('#phaseNext').onclick=()=>{e.remove();render()}}
function renderDetails(){if(!state.activeCourse)return;const host=document.getElementById('today');if(!host)return;let c=document.getElementById('wfDetails');if(!c){c=document.createElement('div');c.id='wfDetails';host.insertBefore(c,host.firstElementChild)}const d=state.courseDetails?.[state.course]||{};c.innerHTML=`<div class="card-title"><div><p class="eyebrow">COURSE DETAILS</p><h3>${esc(course().name)}</h3></div><button class="ghost" id="editDetails">Edit</button></div><div class="course-meta-grid"><div class="course-meta-item"><small>Course number</small><strong>${esc(d.courseNumber||'—')}</strong></div><div class="course-meta-item"><small>Client</small><strong>${esc(d.clientType==='industry'?(d.clientName||'Industry'):'Public')}</strong></div><div class="course-meta-item"><small>Location</small><strong>${esc(d.location||'—')}</strong></div><div class="course-meta-item"><small>Date</small><strong>${esc(d.date||'—')}</strong></div></div>`;c.querySelector('#editDetails').onclick=()=>window.startCourseEdit&&window.startCourseEdit()}
function render(){const old=document.getElementById('todayPhaseCard');if(old)old.remove();renderDetails();const anchor=document.querySelector('#today .grid.three');if(!anchor||!state.activeCourse)return;const phase=currentPhase();const configs={prep:{title:'Preparation checklist',desc:'Get the room, equipment and course materials ready before teaching.',status:'IN PROGRESS',kind:'prep',items:equipmentList()},teach:{title:'Teaching checklist',desc:'Work through the course content. The preparation checklist is complete.',status:'READY TO TEACH',kind:'teach',items:topicItems()},pack:{title:'Pack down checklist',desc:'Finish the course, clean up, document and submit everything.',status:'READY FOR PACK DOWN',kind:'pack',items:packItems()},done:{title:'Course complete',desc:'Preparation, course content and pack down are all complete.',status:'COMPLETE',kind:null,items:[]}};const cfg=configs[phase];const collapseKey=state.course+'-'+phase;const collapsed=phase!=='done'&&!!state.todayCollapsed[collapseKey];let rows='';if(phase==='done')rows='<div class="phase-complete-message">✓ All three course phases are complete for today.</div>';else if(!collapsed)rows=cfg.items.map((item,i)=>{const raw=cfg.kind==='prep'?item:item.value;const text=cfg.kind==='prep'?(raw.text||raw):(Array.isArray(raw)?raw[0]:raw);const detail=Array.isArray(raw)?(raw[1]||''):'';const checked=cfg.kind==='prep'?!!raw.done:cfg.kind==='teach'?!!((state.checks[state.course]||{})[i]):!!((state.packChecks[state.course]||{})[i]);return `<div class="phase-row ${checked?'done':''}" data-index="${i}"><input type="checkbox" ${checked?'checked':''} aria-label="Complete item"><span class="phase-label">${esc(text)}${detail?`<span class="phase-detail">${esc(detail)}</span>`:''}</span><span class="phase-controls"><button type="button" class="phase-up">▲</button><button type="button" class="phase-down">▼</button><button type="button" class="phase-delete">✕</button></span></div>`}).join('');const type=cfg.kind==='prep'?'equipment':cfg.kind==='teach'?'teaching':'packdown';card=document.createElement('div');card.id='todayPhaseCard';card.className='card';card.innerHTML=`<div class="phase-banner"><div><p class="eyebrow">TODAY'S ACTIVE CHECKLIST</p><h3>${cfg.title}</h3><small>${cfg.desc}</small></div><div style="display:flex;align-items:center;gap:8px"><span class="phase-status ${phase==='done'?'complete':''}">${cfg.status}</span>${phase!=='done'?`<button type="button" class="phase-collapse" id="todayPhaseCollapse">${collapsed?'＋':'−'}</button>`:''}</div></div><div class="today-phase-list">${rows}</div>${phase!=='done'&&!collapsed?`<div class="phase-actions"><button type="button" class="primary select-all" id="todaySelectAll">✓ Select all</button><button type="button" class="primary" id="todayPhaseAdd">+ Add item</button><button type="button" class="ghost" onclick="printChecklist('${type}')">🖨 Print checklist</button><button type="button" class="ghost" onclick="emailChecklist('${type}')">✉ Email a copy</button></div>`:''}`;anchor.insertAdjacentElement('afterend',card);if(phase!=='done'&&!collapsed){card.querySelector('#todayPhaseCollapse').onclick=()=>toggleCollapsed(phase);card.querySelector('#todaySelectAll').onclick=()=>{selectAll(cfg.kind);if(currentPhase()!==phase){if(currentPhase()==='teach')transition('Preparation checklist complete','Everything is packed and ready.','Ready to teach','teach');else if(currentPhase()==='pack')transition('Teaching checklist complete','The teaching content is complete.','Ready for pack down','pack');else if(currentPhase()==='done')transition('Pack down checklist complete','The course checklists are complete.','Finish course and create summary','done')}};card.querySelectorAll('input[type=checkbox]').forEach(box=>box.onchange=()=>{const i=Number(box.closest('.phase-row').dataset.index);toggle(cfg.kind,i,box.checked);if(currentPhase()!==phase){const p=currentPhase();if(p==='teach')transition('Preparation checklist complete','Everything is packed and ready.','Ready to teach','teach');else if(p==='pack')transition('Teaching checklist complete','The teaching content is complete.','Ready for pack down','pack');else if(p==='done')transition('Pack down checklist complete','The course checklists are complete.','Finish course and create summary','done')}});card.querySelectorAll('.phase-up').forEach(b=>b.onclick=()=>reorder(cfg.kind,Number(b.closest('.phase-row').dataset.index),-1));card.querySelectorAll('.phase-down').forEach(b=>b.onclick=()=>reorder(cfg.kind,Number(b.closest('.phase-row').dataset.index),1));card.querySelectorAll('.phase-delete').forEach(b=>b.onclick=()=>deleteItem(cfg.kind,Number(b.closest('.phase-row').dataset.index)));card.querySelector('#todayPhaseAdd').onclick=()=>addItem(cfg.kind)}}
if(typeof window.updateAll==='function'){const base=window.updateAll;window.updateAll=function(){base();render()}}
document.addEventListener('DOMContentLoaded',render);setTimeout(render,50);
})();