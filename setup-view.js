(()=>{const style=document.createElement('style');style.textContent=`#setupViewCard{margin-top:0}.setup-row{display:flex;align-items:center;gap:12px;padding:14px 4px;border-top:1px solid #394650;min-height:66px}.setup-row input{width:32px;height:32px;min-width:32px;accent-color:#35c978;cursor:pointer}.setup-row.done .setup-label{text-decoration:line-through;color:#7f8993}.setup-label{flex:1;font-size:18px;line-height:1.35;font-weight:750}.setup-controls{display:flex;gap:5px}.setup-controls button{width:48px;height:48px;border:1px solid #43515d;background:#202a33;color:#d8e0e6;border-radius:10px;font-weight:900;font-size:18px;cursor:pointer}.setup-controls .setup-delete{color:#ff9a9f;border-color:#684047;background:#2b2529}.setup-actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px;padding-top:16px;border-top:1px solid #394650}`;document.head.appendChild(style);
function active(){return !!state.activeCourse&&state.activeCourse===state.course}function items(){return typeof classroomItems==='function'?classroomItems():[]}function checks(){state.classroomChecks=state.classroomChecks||{};state.classroomChecks[state.course]=state.classroomChecks[state.course]||{};return state.classroomChecks[state.course]}function saveSetup(){localStorage.setItem(stateKey,JSON.stringify(state));window.updateAll?.()}function complete(){const a=items(),c=checks();return a.length>0&&a.every((_,i)=>!!c[i])}
function renderSetupView(){const host=document.getElementById('setup');if(!host||!active())return;let card=document.getElementById('setupViewCard');if(!card){card=document.createElement('div');card.id='setupViewCard';card.className='card';host.appendChild(card)}const a=items(),c=checks();card.innerHTML=`<div class="card-title"><div><p class="eyebrow">CLASSROOM SETUP</p><h3>${esc(course().name)} — Setup checklist</h3><p class="muted">Prepare the classroom and practical training environment before teaching.</p></div><span class="phase-status ${complete()?'complete':''}">${complete()?'COMPLETE':'IN PROGRESS'}</span></div><div class="today-phase-list">${a.map((x,i)=>`<div class="setup-row ${c[i]?'done':''}" data-index="${i}"><input type="checkbox" ${c[i]?'checked':''}><span class="setup-label">${esc(x.value)}</span><span class="setup-controls"><button type="button" class="setup-up" ${i===0?'disabled':''}>▲</button><button type="button" class="setup-down" ${i===a.length-1?'disabled':''}>▼</button><button type="button" class="setup-delete">✕</button></span></div>`).join('')}</div><div class="setup-actions"><button type="button" class="primary" id="setupViewSelectAll">✓ Select all</button><button type="button" class="primary" id="setupViewAdd">+ Add item</button></div>`;card.querySelector('#setupViewSelectAll').onclick=()=>{state.classroomChecks[state.course]={};a.forEach((_,i)=>state.classroomChecks[state.course][i]=true);saveSetup();renderSetupView()};card.querySelector('#setupViewAdd').onclick=()=>{const n=prompt('Name the new Setup checklist item:');if(!n||!n.trim())return;state.classroomCustom=state.classroomCustom||{};state.classroomCustom[state.course]=state.classroomCustom[state.course]||[];state.classroomCustom[state.course].push(n.trim());saveSetup();renderSetupView()};card.querySelectorAll('input[type=checkbox]').forEach(box=>box.onchange=()=>{const i=Number(box.closest('.setup-row').dataset.index);checks()[i]=box.checked;saveSetup();renderSetupView()});card.querySelectorAll('.setup-up,.setup-down').forEach(btn=>btn.onclick=()=>{const i=Number(btn.closest('.setup-row').dataset.index),d=btn.classList.contains('setup-up')?-1:1,a2=items(),j=i+d;if(j<0||j>=a2.length)return;const order=a2.map(x=>x.key);[order[i],order[j]]=[order[j],order[i]];state.classroomOrder=state.classroomOrder||{};state.classroomOrder[state.course]=order;saveSetup();renderSetupView()});card.querySelectorAll('.setup-delete').forEach(btn=>btn.onclick=()=>{const i=Number(btn.closest('.setup-row').dataset.index),x=items()[i];if(!x||!confirm('Delete this Setup checklist item?'))return;state.classroomDeleted=state.classroomDeleted||{};state.classroomDeleted[state.course]=state.classroomDeleted[state.course]||{};state.classroomDeleted[state.course][x.key]=true;saveSetup();renderSetupView()})}
window.renderSetupView=renderSetupView;
document.addEventListener('click',e=>{const b=e.target.closest('#stageClassroom');if(!b)return;if(!active())return;e.preventDefault();e.stopImmediatePropagation();if(typeof showView==='function')showView('setup');else{document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));document.getElementById('setup')?.classList.add('active')}setTimeout(renderSetupView,80)},true);
document.addEventListener('DOMContentLoaded',()=>setTimeout(renderSetupView,200));const oldUpdate=window.updateAll;if(oldUpdate&&!oldUpdate.__setupView){window.updateAll=function(){oldUpdate();renderSetupView()};window.updateAll.__setupView=true}
})();
