(()=>{
function wire(){const host=document.getElementById('courseCards');if(!host)return;host.querySelectorAll('h3').forEach(h=>{const name=h.textContent.trim(),id=Object.keys(courses).find(k=>courses[k].name===name);if(!id)return;const card=h.closest('.course-card')||h.parentElement?.parentElement;if(!card||card.querySelector('.course-stage-actions'))return;const box=document.createElement('div');box.className='course-stage-actions';box.innerHTML='<button class="teach-now">▶ Teach</button><button>▣ Classroom Setup</button>';box.children[0].onclick=()=>{state.course=id;state.activeCourse=id;if(typeof save==='function')save();document.querySelector('.tab[data-view="today"]')?.click();setTimeout(()=>document.getElementById('stageTeach')?.click(),100)};box.children[1].onclick=()=>{state.course=id;state.activeCourse=id;if(typeof save==='function')save();document.querySelector('.tab[data-view="today"]')?.click();setTimeout(()=>document.getElementById('stageClassroom')?.click(),100)};card.appendChild(box)})}document.addEventListener('DOMContentLoaded',wire);setInterval(wire,700)})();